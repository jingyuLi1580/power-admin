server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/power?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
  redis:
    host: 127.0.0.1
    port: 6379
    timeout: 20000
  jedis:
    pool:
      max-active: 8
      min-idle: 0
      max-idle: 8
      max-wait: -1
    database: 0
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  #QQ邮箱邮件发送服务配置（固定写法）
  mail:
    host: smtp.qq.com
    port: 587
    # 配置个人QQ账户和密码（密码是加密后的授权码）
    username: 956291084@qq.com
    password: paubppgqtaetbgac
  mvc:
    servlet:
      load-on-startup: 1

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    auto-mapping-behavior: full
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapper/**/*Mapper.xml
  global-config:
    db-config:
      logic-not-delete-value: 1
      logic-delete-value: 0
  type-aliases-package: com.example.powerAdmin.entity


identity:
  name: lijingyu
  age: boy

